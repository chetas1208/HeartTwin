# The held-out split is regenerated by train.py (it indexes the gitignored
# PTB-XL dataset). The trained model.joblib IS committed so the API endpoint
# works out of the box.
test_split.npz
__pycache__/
